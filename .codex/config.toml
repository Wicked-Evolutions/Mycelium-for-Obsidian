# Codex project orchestration/installation policy, not canonical role semantics.
# Role provenance and current effectiveness state: docs/development/AI_TEAM_DISTRIBUTION.json
[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800
interrupt_message = true
