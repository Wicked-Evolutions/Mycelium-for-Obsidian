node_modules/
dist/
.env
.env.*
!.env.example
.npmrc
*.log
*.mcpb
.mcpb-build/
.DS_Store
data/*.db

# IJFW local agent layer (team/charter/blackboard + Codex projection) — not committed
.ijfw/
.codex/

# Understand-Anything — commit the graph data (knowledge-graph.json, meta.json,
# fingerprints.json, .understandignore) so the dashboard CI can publish it;
# ignore scratch, local config, and any local static build.
.understand-anything/intermediate/
.understand-anything/tmp/
.understand-anything/.trash-*/
.understand-anything/dashboard-static/
.understand-anything/config.json

# Local design materials / scratch — not part of the package
prototypes/
node_modules
